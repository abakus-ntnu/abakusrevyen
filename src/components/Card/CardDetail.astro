---
const { class: className } = Astro.props;
---

<div class="flex flex-col gap-1.5">
  <slot name="title" />
  <div
    class:list={[
      "flex items-center gap-2 rounded-md bg-gray-100 p-1.5 transition-colors group-hover/linkcard:bg-gray-200 dark:bg-gray-800 group-hover/linkcard:dark:bg-gray-700",
      className,
    ]}
  >
    <slot />
  </div>
</div>
