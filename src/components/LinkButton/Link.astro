---
const { href, class: className, ...rest } = Astro.props;
---

<a
  href={href}
  class:list={[
    "text-primary underline transition-colors hover:text-secondary",
    className,
  ]}
  {...rest}><slot /></a
>
