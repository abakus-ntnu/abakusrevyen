---
interface Props {
  class: string;
  date: Date | string;
}

const { class: className, date } = Astro.props;
const now = new Date();
---

<li class={now < date ? "opacity-100" : "opacity-50"}>
  <div class="flex items-center gap-3">
    <div class="aspect-square w-3 rounded-full bg-primary"></div>
    <span class="font-medium">
      {
        date.toLocaleString("nb-NO", {
          day: "numeric",
          month: "long",
          year: "numeric",
          hour: "numeric",
          minute: "2-digit",
        })
      }
    </span>
  </div>
  <div
    class="ml-1.5 border-l-[1px] border-dashed border-foreground pl-[1.125rem] pr-3"
  >
    <div class:list={["py-3", className]}>
      <slot />
    </div>
  </div>
</li>
